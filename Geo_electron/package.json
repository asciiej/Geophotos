{
  "name": "photo-processor",
  "version": "1.0.0",
  "description": "App para processar fotos com dados EXIF",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . PhotoProcessor --platform=win32 --arch=x64 --out=dist"
  },
  "devDependencies": {
    "electron": "^25.0.0",
    "electron-packager": "^17.0.0"
  },
  "dependencies": {
    "sharp": "^0.32.1",
    "fs-extra": "^11.1.0",
    "node-exiftool": "^6.0.0"
  }
}
